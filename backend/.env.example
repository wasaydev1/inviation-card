PORT=4000
# Listen address (0.0.0.0 = Wi‑Fi / LAN). Tighter dev-only: HOST=127.0.0.1
HOST=0.0.0.0
# Required for saving finished races (optional — server runs without it; history won’t persist)
MONGODB_URI=mongodb://127.0.0.1:27017/type-arena
